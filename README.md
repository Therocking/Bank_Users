# Iniciar projecto

1. Agregar el `Connetionstring` de la db en el archivo de configuración(en el espacio `DefaultConnetion`).
2. Ejecutar `update-database` para crear la db en local.
3. Iniciar el proyecto